package im.dig.trial.messenger.services

package object model {
  type HashId = SHA256
  type UserId = HashId
  type SessionId = HashId
  type MessageId = HashId
  type FileId = HashId
  type ChatId = HashId
}
